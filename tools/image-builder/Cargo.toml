[package]
name = "image-builder"
version = "0.1.0"
edition = "2024"

[build-dependencies]
bootloader = { version = "0.11.17", default-features = false, features = ["bios"] }
unbound-kernel = { path = "../../kernel", artifact = "bin", target = "x86_64-unknown-none" }
